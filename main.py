# ── main.py ───────────────────────────────────────────────────────────────────
# Entry point. Run this file to launch the app:
#   python main.py

from app import FlipCardApp

if __name__ == "__main__":
    app = FlipCardApp()
    app.mainloop()
